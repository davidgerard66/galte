{% extends "::layout/layoutAdmin.html.twig" %}

{% block body -%}
    <h1>Les photos </h1>

    <table class="table table-striped table-hover">
        <thead>
            <tr>

                <th>Gite</th>
                <th>Nom</th>
                <th>Photo</th>
                <th>Ordre</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>

                <td>n° {{ entity.id }} {{ entity.gite.nom }}</td>
                <td>{{ entity.name }}</td>
                <td><img src='{{ pathDefault ~ entity.getWebPath }}' width="60"></td>
                <td>{{ entity.ordre }}</td>
                <td>

                        <a href="{{ path('admin_media_show', { 'id': entity.id }) }}"><i class="glyphicon glyphicon-info-sign"></i></a>

                        <a href="{{ path('admin_media_edit', { 'id': entity.id }) }}"><i class="glyphicon glyphicon-edit"></i></a>

                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>


            <a href="{{ path('admin_media_new') }}" class="btn btn-success">
                Ajouter une nouvelle photo
            </a>

    {% endblock %}
