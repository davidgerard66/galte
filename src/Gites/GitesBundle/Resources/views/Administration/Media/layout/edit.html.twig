{% extends "::layout/layoutAdmin.html.twig" %}
{% form_theme edit_form 'bootstrap_3_layout.html.twig' %}
{% block body -%}
    <h1>Modifier la photo</h1>
    <div class="container-fluid">
        <div class="row">
            <div class="col-md-6">
                {{ form(edit_form) }}
                <br><br>
                <ul class="list-unstyled list-inline">
                    <li>
                        <a href="{{ path('admin_media') }}" class="btn btn-default">
                            retour à la liste des photos
                        </a>
                    </li>
                    <li class="pull-right">{{ form(delete_form) }}</li>
                </ul>
            </div>
            <div class="col-md-4 col-md-offset-1">
                <br>
                <img src='{{ pathDefault ~ entity.getWebPath }}' style="width:400px;" class="img-thumbnail">
            </div>
        </div>
    </div>




{% endblock %}
